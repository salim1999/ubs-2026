# Python
__pycache__/
*.py[cod]
.venv/

# Extracted data (the zips are tracked; regenerate with `python -m src.features`)
data/dataset/
data/processed/

# Too large for GitHub (>100 MB); regenerate from data/dataset.zip
data/unlabeled_pretrain_transactions.jsonl
